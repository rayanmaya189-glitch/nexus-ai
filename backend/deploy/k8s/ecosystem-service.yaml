apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecosystem-service
  namespace: nexus-ai
  labels:
    app: ecosystem-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ecosystem-service
  template:
    metadata:
      labels:
        app: ecosystem-service
    spec:
      containers:
        - name: ecosystem-service
          image: nexus-go/ecosystem-service:latest
          ports:
            - containerPort: 8089
          env:
            - name: PORT
              value: "8089"
            - name: DB_HOST
              value: "postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "nexus"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: db-password
            - name: DB_NAME
              value: "nexus_main"
            - name: DB_SSLMODE
              value: "disable"
          readinessProbe:
            httpGet:
              path: /health
              port: 8089
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: ecosystem-service
  namespace: nexus-ai
spec:
  selector:
    app: ecosystem-service
  ports:
    - port: 8089
      targetPort: 8089
  type: ClusterIP
