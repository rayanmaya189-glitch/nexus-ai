apiVersion: apps/v1
kind: Deployment
metadata:
  name: memory-service
  namespace: nexus-ai
  labels:
    app: memory-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: memory-service
  template:
    metadata:
      labels:
        app: memory-service
    spec:
      containers:
        - name: memory-service
          image: nexus-go/memory-service:latest
          ports:
            - containerPort: 8091
          env:
            - name: PORT
              value: "8091"
            - name: DB_HOST
              value: "postgres"
            - name: DB_PORT
              value: "5432"
            - name: DB_USER
              value: "nexus"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: nexus-secrets
                  key: db-password
            - name: DB_NAME
              value: "nexus_main"
            - name: DB_SSLMODE
              value: "disable"
          readinessProbe:
            httpGet:
              path: /health
              port: 8091
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "256Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: memory-service
  namespace: nexus-ai
spec:
  selector:
    app: memory-service
  ports:
    - port: 8091
      targetPort: 8091
  type: ClusterIP
